# Conexao com a base de dados.
# SQLite (padrao, nao precisa de servidor):
DATABASE_URL=sqlite:///./cartoes.db

# Para trocar para PostgreSQL depois, basta comentar a linha acima e usar:
# DATABASE_URL=postgresql+psycopg2://usuario:senha@localhost:5432/cartoes
